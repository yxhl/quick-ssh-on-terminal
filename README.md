# quick-ssh-on-terminal README

Open ssh connection on terminal

## Features

Click connect button to open ssh connection on terminal, ssh session is based on `ssh_config`

## Release Notes

### 0.0.1

Init